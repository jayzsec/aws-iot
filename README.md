# AWS IoT

This project explores the underlying infrastructure that supports IoT monitoring.

## Cloud Architecture

![AWS Cloud Architecture Diagram](./docs/aws-iot-v2.svg "AWS Architecture Overview")

> *Figure 1: High-level architecture showing flow between different services such as iot, api gateway, and database.*

## Prerequisite

- **AWS CLIv2**: login using `aws login`. Alternatively, use `aws_access_key_id` and `aws_secret_access_key`.
- **aws-session-manager-plugin**: To securely connect to resources.

