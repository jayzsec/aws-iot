services:
  sim-01:
    build: .
    environment:
      AWS_IOT_ENDPOINT: "${AWS_IOT_ENDPOINT}"
      CLIENT_ID: "iot-fleet-platform-dev-sim-01"
    volumes:
      - ./certs:/app/certs:rw
      - ./device_sim.py:/app/device_sim.py:ro
      - ./fleet_provision.py:/app/fleet_provision.py:ro

  sim-02:
    build: .
    environment:
      AWS_IOT_ENDPOINT: "${AWS_IOT_ENDPOINT}"
      CLIENT_ID: "iot-fleet-platform-dev-sim-02"
    volumes:
      - ./certs:/app/certs:rw
      - ./device_sim.py:/app/device_sim.py:ro
      - ./fleet_provision.py:/app/fleet_provision.py:ro
